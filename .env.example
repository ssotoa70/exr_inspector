# exr-inspector Deployment Configuration Template
# Copy this file to .env and fill in your VAST cluster details
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your values
#   ./deploy.sh --config .env

##############################################################################
# VAST Cluster Configuration
##############################################################################

# VAST cluster URL (https://your-vast-cluster.internal or IP)
VAST_CLUSTER_URL="https://vast-cluster.internal"

# VAST API Key (from VAST admin)
VAST_API_KEY="your-api-key-here"

# VAST Tenant name (default is usually "default")
VAST_TENANT_NAME="default"

##############################################################################
# VAST DataBase Configuration (Required for persistence)
##############################################################################

# VAST DataBase S3 endpoint
# Example: s3.us-east-1.vastdata.com or s3.vastdb.internal
VAST_DB_ENDPOINT="s3.region.vastdata.com"

# VAST DataBase access key (AWS-style credentials)
VAST_DB_ACCESS_KEY="your-access-key"

# VAST DataBase secret key
VAST_DB_SECRET_KEY="your-secret-key"

# AWS region for S3 endpoint construction
VAST_DB_REGION="us-east-1"

# Schema name in VAST DataBase (will be created if not exists)
VAST_DB_SCHEMA="exr_metadata"

##############################################################################
# Container Registry Configuration
##############################################################################

# Registry URL (docker.io for Docker Hub, or your private registry)
# Examples:
#   docker.io (Docker Hub)
#   ghcr.io (GitHub Container Registry)
#   ecr.us-east-1.amazonaws.com (AWS ECR)
#   registry.company.internal (Private registry)
REGISTRY_URL="docker.io"

# Registry username (for authentication)
REGISTRY_USERNAME="your-registry-user"

# Registry password or token
REGISTRY_PASSWORD="your-registry-password"

# Image repository path (namespace/name)
# Format: org/exr-inspector or my-account/exr-inspector
IMAGE_REPOSITORY="my-org/exr-inspector"

# Image tag/version
IMAGE_TAG="v1.0.0"

##############################################################################
# S3/Object Storage Configuration
##############################################################################

# S3 bucket name for input EXR files (trigger source)
# This is the bucket where EXR files will be uploaded
S3_BUCKET="exr-input-data"

##############################################################################
# Optional: Kubernetes Configuration
##############################################################################

# If using custom Kubernetes cluster (usually managed by VAST)
# K8S_CLUSTER_NAME="exr-processor"
# K8S_NAMESPACE="dataengine"

##############################################################################
# Optional: Advanced Configuration
##############################################################################

# Function timeout (seconds)
# FUNCTION_TIMEOUT="300"

# Function memory (MB)
# FUNCTION_MEMORY="1024"

# Enable debugging/verbose logging
# DEBUG="true"

# Staging environment testing (before production)
# STAGING_MODE="true"
