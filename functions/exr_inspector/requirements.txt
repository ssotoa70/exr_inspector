# Python dependencies for the exr-inspector DataEngine function
#
# OpenImageIO is provided by the system package libopenimageio-dev (see Aptfile).
# The Python bindings are included in that package and do NOT need a pip install.
#
# VAST DataBase persistence dependencies:
pyarrow>=10.0.0
vastdb>=1.3.9
